package voicetext

// speaker represents a speaker of the API.
type speaker struct {
	// name represents the speaker's name.
	name string
	// canUseEmotion indicates that the speaker can use emotion or not.
	canUseEmotion bool
}
